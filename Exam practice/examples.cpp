#include <iostream>
#include <deque>
#include <string>
using namespace std;

bool isPalindrome(const string &str)
{
    string filteredstr;
    deque<char> 
    for(character ch: str)
        filteredstr += tolower(ch);
    n = filteredstr.length();

    for(i=0; i<n/2;i++)
    {

    }
    return
}

void main()
{
    deque<ch> str;
}